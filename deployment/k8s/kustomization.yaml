apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pet-adoption

resources:
- namespace.yaml
- deployment.yaml
- service.yaml
- hpa.yaml
- networkpolicy.yaml
- pdb.yaml
  # - ingress.yaml     # uncomment if your cluster has an ingress controller
  # - secret.yaml      # copy from secret.example.yaml, fill in a real key, then uncomment
                        # (or better: manage this Secret via Sealed Secrets / External
                        # Secrets Operator instead of applying it through this kustomization)

images:
- name: ghcr.io/swapniljain782/dogs-cats-adoption/pet-classifier
  newName: ghcr.io/swapniljain782/dogs-cats-adoption/pet-classifier
  newTag: c5fb7f77bc04c63eee68ea9be12d6e439a976ab5
