apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pet-adoption

resources:
  - namespace.yaml
  - deployment.yaml
  - service.yaml
  - hpa.yaml
  # - ingress.yaml   # uncomment if your cluster has an ingress controller

images:
  - name: ghcr.io/OWNER/REPO/pet-classifier
    newTag: latest
