apiVersion: v1
kind: Secret
metadata:
  name: mlflow-secrets
  namespace: pet-adoption
type: Opaque
stringData:
  # Generate with: openssl rand -base64 32
  postgres-password: "changeme-postgres-password"
  minio-password: "changeme-minio-password"